# TabGate

Always save implementation plans to `docs/plans/` with date-prefixed filenames (e.g. `2026-03-15-feature-name.md`) before execution.

Before committing and pushing, run a security check: review staged changes for secrets, credentials, API keys, tokens, .env files, or other sensitive data. Flag any findings to the user before proceeding.
